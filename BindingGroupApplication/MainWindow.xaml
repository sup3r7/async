<Window x:Class="BindingGroupApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mocker="clr-namespace:async;assembly=async"
        Title="MainWindow" Height="350" Width="525"
        >
    <Window.DataContext>
        <mocker:MockerClass/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <DataTemplate DataType="{x:Type mocker:MockerClass}">
                <StackPanel DataContext="{ TemplateBinding Property=DataContext}">

                    <DataGrid AutoGenerateColumns="True"
                              BindingGroup="{Binding GridBindingGroup}"
                              ItemsSource="{Binding MockerList}"></DataGrid>
                    <TextBox Text=""></TextBox>

                </StackPanel>
            </DataTemplate>

        </Grid.Resources>
        <ContentPresenter Content="{Binding}">

        </ContentPresenter>

    </Grid>
</Window>
